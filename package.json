{
  "name": "unibrain-hook",
  "version": "1.0.0",
  "description": "UniBrain - AI-Powered Dynamic Fee Hook for Uniswap v4 using Reactive Network + NVIDIA Qwen AI",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "uniswap",
    "v4",
    "hooks",
    "reactive-network",
    "nvidia",
    "ai",
    "defi",
    "dynamic-fees"
  ],
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}
